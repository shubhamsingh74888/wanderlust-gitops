apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  namespace: wanderlust
spec:
  accessModes:
    - ReadWriteOnce
  # Points to the EBS StorageClass defined in persistentVolume.yaml
  storageClassName: "wanderlust-ebs"
  resources:
    requests:
      storage: 5Gi
